package main

import (
	"testing"
)

func TestSelectMode(t *testing.T) {
	/*
		cases := []struct {
			args    []string,
		}{
			{["sqlite", "Hello this is a note"]},
			{["sqlite", "view"]},
			{["sqlite", "view", "1"]},
			{["sqlite", "reset"]},

		}
		t.Error("bla")
	*/

}
